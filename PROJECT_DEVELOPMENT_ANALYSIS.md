# textRPG 개발 과정 분석

## 분석 범위와 읽는 법

- **대상 이력:** `599b7b5`(2026-07-16 최초 업로드)부터 `984dd89`(2026-07-31 이벤트 방문 처리 수정)까지, HEAD 기준 123개 커밋이다.
- **근거:** 커밋 메시지, 각 커밋의 파일 diff, HEAD의 C++ 헤더·구현·주석을 함께 대조했다. 기록상 작성자 표기는 `shd` 41건, `user` 85건으로 분리되어 있으나 실명·역할 매핑은 없으므로 아래에서는 이를 한 개발 과정으로 분석한다.
- **주의:** 이 저장소에는 HEAD에서 자동화 테스트 파일과 README가 보이지 않는다. 따라서 “정상 동작”을 단정하지 않고, 실제로 수정된 코드와 그 수정이 겨냥한 문제를 중심으로 평가한다. 현재 프로젝트는 Visual Studio v143 기반의 Windows 콘솔 애플리케이션이다.

## 한눈에 본 개발 방식

이 과정은 처음부터 완성된 설계를 밀어붙인 방식이라기보다, **작은 기능을 빠르게 넣고 실제 결합 지점에서 드러난 문제를 구조 변경으로 흡수하는 반복형 개발**에 가깝다. 초반에는 Actor, 확률, 전투라는 최소 게임 코어를 만들었다. 이후 책임 분리와 상태 모델을 재정의했고, 중반에는 장비·포션·스킬·ID·이벤트 DB를 데이터 중심으로 확장했다. 마지막 이틀은 UI와 전체 게임 루프를 연결한 뒤, 이벤트 중복·체력 의미·회피 판정·몬스터 수치처럼 플레이 경험을 깨는 경계 조건을 연속적으로 수정하는 안정화 단계였다.

커밋이 작고 문제명이 제목에 직접 드러나는 점은 추적에 매우 유리하다. 반면 기능 추가와 리팩터링이 같은 날 촘촘히 얽혀 있고 일부 코드는 주석 처리된 채 남아 있어, “완전히 설계한 후 구현”보다는 “구현하면서 모델을 교정”한 흔적도 분명하다.

## 타임라인: 문제 인식과 해결의 흐름

| 시기 | 확인된 변화 | 마주친 문제와 고민의 방식 | 실제 해결·의미 |
|---|---|---|---|
| 7/16 | `599b7b5` 최초 업로드, `5d40eb8` 추가 스탯 | 게임의 최소 도메인인 캐릭터 능력치부터 시작했다. 이후 기능이 늘어날 것을 전제로 수치 모델을 먼저 잡으려 한 단계다. | 작은 시작점 위에 Actor·스탯 체계를 쌓는 기반을 만들었다. |
| 7/20 | Player를 Actor로 일반화(`38d41e9`), Dice/확률 클래스와 전투·스킬 추가 | 플레이어만을 전제로 하면 몬스터와 전투 규칙을 공유하기 어렵다. 확률과 전투를 독립 개념으로 빼야 기능을 덧붙일 수 있다는 문제가 나타났다. | `Actor`라는 공통 기반을 만들고 확률·전투·스킬을 분리했다. “범용성”이라는 커밋 메시지처럼 상속과 공통 행위를 중심으로 재구성했다. |
| 7/21~7/22 | 행동 인자를 참조로 변경, Actor 단일 책임화(`3924ef0`), Status/Player/Monster/Screen 도입 | Actor가 행위와 상태, 표시까지 떠안으면서 덩치가 커졌다. 특히 `3924ef0`은 Actor.cpp에서 255줄을 줄이는 큰 구조 전환이다. | Status를 별도 객체로 두고 Player·Monster의 역할을 나눴다. virtual 소멸자, 상속 구조, 주석으로 수명·역할 의도를 남겼다. 이는 이후 장비·스킬을 얹기 위한 선행 정리였다. |
| 7/23~7/24 | 프로젝트/인코딩 복구, Status 구조체·enum 접근, setter 검증 분리, Util 통합 | 구현 중 Visual Studio 프로젝트와 유니코드 같은 개발 환경 문제도 처리했다. 수치를 전부 unsigned로 바꿨다가 다시 조정하는 흐름은 “음수 방지”와 전투 피해·보정의 표현성을 함께 고민했음을 보여 준다. | enum 기반 Status API와 setter 경계 검증을 만들고, Dice의 성공 판정을 `Util`로 통합했다. 기능 코드에 흩어진 규칙을 재사용 가능한 유틸리티로 모으려 했다. |
| 7/27 오전 | 확률 정규화·베르누이 분포(`8ae49b2`), ANSI 화면 정리, 상태 비교 | 확률을 정수로 넣되 `0~1` 확률 분포로 계산해야 했고, 화면 출력은 콘솔 잔상과 입력 문제를 야기했다. | HEAD의 `Util`은 입력을 0~100 범위로 clamp한 뒤 `std::bernoulli_distribution`을 쓰며, 공격자/방어자 비교도 별도 함수로 둔다. ANSI 화면 정리와 뒤이은 좌표 출력 도입은 게임 흐름을 눈으로 검증하기 위한 선택이다. |
| 7/27 오후 | HP/MP, 포션 DB, 장비 기본 데이터와 사용자 강화, 몬스터 FSM, 전투 보상 | 전투가 단순 공격만으로는 확장되지 않았다. 아이템의 변하지 않는 원본 정보와 플레이어별 강화 상태를 같은 객체로 들고 있으면 교체·보상 시 책임이 섞인다. | `EquipmentData`와 장착 슬롯을 분리하고 `shared_ptr<const ...>`로 공용 원본을 보호하며, 강화 레벨·계산 스탯은 슬롯이 관리하게 했다. 몬스터는 체력·방어·스킬·포션 상태에 따라 행동을 고르는 FSM을 얻었다. |
| 7/28 | Item 템플릿화, 장비/포션/스킬 ID 규칙, 스킬 콜백, 보상 선택 | 데이터 종류가 늘면서 단순 배열 인덱스만으로는 아이템·스킬·이벤트를 식별하기 어려워졌다. 스킬 효과도 이름별 분기보다 규칙 데이터에 붙이는 편이 확장 가능했다. | 카테고리·등급·소유자를 조합하는 ID 생성 함수와 map DB를 도입했다. `SkillData::effect`에 시전자와 타깃을 받는 `std::function`을 둬, 스킬 정의와 전투 실행을 느슨하게 연결했다. |
| 7/29 | enum class로 인한 map 전환, const reference 인벤토리, 플레이어/몬스터 행동 분리, 전방 선언, UI 프레임·이벤트 클래스 | 타입 안정성을 높인 `enum class`는 기존 int 배열 접근을 깨뜨렸고, `Actor`와 `Skill`의 상호 include는 순환 의존을 만들었다. 입력 메뉴는 하위 메뉴에서 취소할 때 이전 상태를 복원해야 했다. | 배열을 map으로 바꾸고, 인벤토리는 const reference로 노출했으며, `class Actor;` 전방 선언으로 의존을 끊었다. Battle은 메뉴 상태를 stack으로 관리하고, GameManager 콜백으로 UI를 분리했다. |
| 7/30 오전 | 랜덤 이벤트, 게임 순환, 몬스터 생성, 직전 이벤트 회피, 전투 로그 | 개별 이벤트가 있어도 게임 전체가 이어지지 않으며, 무작위 선택은 같은 이벤트가 연속될 수 있다. 전투의 내부 진행도 UI에 보여야 플레이가 가능하다. | Event DB에서 후보를 골라 GameManager 루프에서 소비하고, 직전 ID를 제외했다. Battle은 UI 갱신 콜백과 최근 로그를 두어 도메인 결과를 화면으로 전달했다. |
| 7/30 오후 | 이벤트 선형 연결(`6749afb`), max/방문/논리 수정, 현재·최대 체력 분리(`7188945`), 좌표 상수/종료 통합 | 랜덤만으로는 서사·난이도 흐름이 불안정했고, 이벤트 중복/소진과 화면 범위가 엮이며 논리 오류가 발생했다. 무엇보다 “최대 체력”과 “현재 체력”을 하나로 다루면 회복·피해·표시가 충돌한다. | `EventResult::nextEvent`로 선택마다 다음 이벤트를 명시했다. `EventData::isVIsited` 필터와 `setVisited`를 넣고, Actor는 `tmpHp/fullHp`, `tmpMp/fullMp`로 상태를 분리했다. 명칭은 다소 임시적이지만 모델을 둘로 나눈 결정 자체가 핵심 개선이다. |
| 7/31 | 미스/크리티컬 헬퍼 제거(`01717bc`), 회피 적용 수정(`66b4e81`), 몬스터 스탯·이벤트 방문 처리 수정(`984dd89`) | 기능이 결합된 뒤에 가장 중요한 게임 규칙의 순서 문제가 드러났다. 기존에는 미스여도 HP 갱신이 이루어질 수 있었고, 회피/크리티컬 확률의 의미와 로그 단위도 일관되지 않았다. 이벤트 루프 역시 nextEvent 처리와 방문 기록 시점이 맞물렸다. | `doAttack`에서 회피를 가장 먼저 판정하고 빗나가면 즉시 0을 반환하도록 고쳤다. 이후에만 치명타·피해·HP 갱신을 수행한다. GameManager에서는 다음 이벤트 선택, Empty 판정, 방문 기록 순서를 재배치하고 레벨업 UI 갱신도 보완했다. 이는 “증상 수정”에서 끝내지 않고 실행 순서 자체를 고친 사례다. |

## 코드와 주석에서 읽히는 설계 습관

1. **의도를 먼저 적고 구현으로 옮긴다.** `Battle.cpp`의 회피→치명타→로그→체력 반영 순서 주석, `GameManager.h`의 좌표 기반 화면 도식, `Event.cpp`의 DB 구획 주석은 단순 설명보다 구현 순서와 화면/도메인 경계를 드러낸다. 특히 7월 31일 회피 수정은 주석과 코드 순서가 일치한다.
2. **문제가 보이면 책임을 옮긴다.** Dice를 Util로, Screen을 GameManager로, 고정 장비 데이터와 장착 상태를 서로 다른 객체/슬롯으로 옮겼다. Actor와 Skill의 순환 참조는 전방 선언으로 좁혀 해결했다. 즉, 같은 파일에 임시 분기를 더하기보다 관계를 재배치하는 선택이 반복된다.
3. **콜백을 경계로 UI와 규칙을 연결한다.** 스킬 효과, 전투 로그, 전투 화면 갱신, 장비 보상 선택에 `std::function`을 활용한다. 콘솔 UI라는 제약 안에서 GameManager가 입력·표시를 맡고 Battle이 전투 상태를 맡으려 한 방향이다.
4. **다만 주석은 정리 단계가 한 번 더 필요하다.** HEAD에는 제거한 `criticalCheck`, `missCheck`, 이전 Equipment 구현, 과거 확률식이 주석 처리된 채 남아 있다. 주석이 설계 의도를 돕는 장점은 크지만, 폐기 코드가 같이 있으면 현재 규칙을 읽는 사람이 혼란스러워진다.

## 강점 (500자 이상)

이 프로젝트의 가장 큰 강점은 기능을 빠르게 늘리는 와중에도 **문제를 발견한 지점을 구조 개선의 계기로 삼는 태도**다. 7월 20일 Player를 Actor로 일반화한 뒤, 7월 22일에는 Actor의 책임을 다시 줄이고 Status·Player·Monster를 분리했다. 이는 처음 선택을 고집하기보다, 새 요구가 생겼을 때 기존 추상화가 충분한지를 되돌아본 결과다. 7월 27일 장비에서도 원본 DB 데이터와 플레이어별 강화 상태를 분리했고, 7월 28일 스킬은 효과 함수를 데이터에 담아 전투 코드의 조건 분기를 줄이려 했다. 이런 흐름은 객체의 수를 늘리기 위한 추상화가 아니라, 변경되는 값과 변하지 않는 값을 나누려는 실질적인 판단으로 보인다. 메모리 관리에서도 `unique_ptr`로 몬스터 수명을 전투 반복 범위에 묶고, 공용 데이터는 `shared_ptr<const ...>`로 읽기 전용임을 표현한 점이 좋다. 마지막 안정화의 품질도 높다. 회피 수정은 단순히 확률값을 바꾸지 않고, “회피 성공 시 피해 계산과 HP 변경이 절대 일어나지 않아야 한다”는 제어 흐름을 early return으로 명확히 만들었다. 이벤트 방문 처리도 후보 필터, 현재 이벤트 ID, nextEvent, 종료 판정을 함께 다루며 전체 루프의 순서를 재검토했다. 커밋 제목이 비교적 구체적이고 작은 수정 단위로 나뉘어 있어, 어떤 결함이 어떤 변경으로 이어졌는지 추적하기 쉽다. 주석도 화면 좌표, DB ID 규칙, 상태 전이 같은 비자명한 의도를 남기므로 학습·협업 비용을 낮춘다. 요약하면 이 개발자는 구현 속도만 추구하지 않고, 실제로 막힌 지점에서 데이터 소유권·상태 모델·실행 순서를 재설계할 줄 안다는 강점이 있다.

## 고쳐야 할 점 (500자 이상)

가장 먼저 고쳐야 할 점은 **수정의 정확성을 자동으로 증명하는 장치가 없는 것**이다. HEAD에는 테스트 파일이 없고, 확률·전투·이벤트 선택처럼 재현이 어려운 규칙을 수동 플레이와 로그에 의존하는 모습이다. 그 결과 `8ae49b2`의 diff에는 정규화 변수와 다른 이름의 `success_rate`를 다시 clamp하는 중간 상태가 보이고, 회피/치명타는 7월 31일에 연속 수정됐다. 커밋이 작다는 장점이 이 경우에는 임시 상태가 주 이력에 남고, 인접 커밋 없이는 컴파일·동작 단위를 보장하지 못한다는 약점으로 바뀐다. 다음으로 명명과 코드 정리의 일관성이 부족하다. `isVIsited`, `DrowFillLine`, `EquipmnetEnhance`, 과거 `dobleToStr` 흔적, `tmpHp/fullHp` 등은 의미를 추론하게 하거나 오타를 퍼뜨린다. `Battle.h`, `Equipment.cpp`, `Player.cpp`, `Util.cpp`에는 이미 폐기된 함수와 구현이 큰 주석 블록으로 남아 있어, 현재 코드와 과거 실험을 구분하기 어렵다. 주석은 “왜”를 남기고 삭제된 구현은 Git에 맡기는 편이 낫다. 또한 `GameManager`는 Windows API 좌표 출력, 입력 검증, 화면 프레임, 이벤트 진행, 전투 시작, 레벨업 UI를 함께 갖고 있어 책임이 과도하다. Event DB의 정적 map과 방문 상태도 전역적 수명이라 새 게임 시작 시 초기화 정책이 명확하지 않다. 확률 함수는 랜덤 엔진을 내부에서 직접 만들기 때문에 seed 고정 테스트가 어렵고, 0 대 0 비교를 실패로 정한 게임 규칙도 호출부에서 명확히 드러나지 않는다. 마지막으로, ID를 정수 덧셈 규칙으로 직접 만들고 enum 값을 암묵적으로 조합하는 방식은 편하지만 범위 충돌과 잘못된 ID를 컴파일 단계에서 막지 못한다. 이 문제들은 기능을 더하기 전에 경계와 계약을 작게 명문화하면 상당 부분 줄일 수 있다.

## 강화해야 할 점 (500자 이상)

앞선 강점을 지속 가능한 품질로 키우려면 **도메인 규칙을 테스트 가능한 단위로 고정하고, UI 바깥에서 실행 가능한 게임 상태 전이로 강화**해야 한다. 첫 단계로 `Util`, `Battle`, `Event`, `Actor/Player`에 단위 테스트를 둬야 한다. 예를 들어 회피 성공이면 HP가 변하지 않는지, 피해가 항상 0 이상인지, 최대 HP를 넘는 회복이 막히는지, 방문한 이벤트와 직전 이벤트가 후보에서 빠지는지, 모든 이벤트가 소진됐을 때 Empty 이벤트로 안전하게 끝나는지를 고정할 수 있다. 확률 자체는 난수 엔진 또는 난수 공급자를 주입해 seed를 고정하고, 분포 검증은 많은 시행의 허용 오차 테스트로 분리하면 된다. 둘째, `GameManager`에서 콘솔 입출력과 게임 진행을 나누는 방향을 강화해야 한다. Event 선택 결과를 받아 다음 상태를 반환하는 `GameSession` 또는 `GameFlow`를 순수 C++ 객체로 만들고, GameManager는 그 상태를 화면에 그리며 입력만 전달하게 하면 Windows 콘솔 의존 없이 규칙을 검사할 수 있다. 셋째, 지금의 데이터 중심 설계를 한 단계 더 밀어야 한다. 장비·스킬·이벤트에 이어 몬스터 수치와 보상도 DB 데이터로 일관되게 표현하고, ID는 강한 타입 또는 `enum class`와 검증 함수로 감싸 잘못된 조합을 초기에 거부하는 편이 안전하다. 넷째, 커밋 전에 빌드와 테스트를 실행하는 짧은 체크리스트, 함수·파일 명명 규칙, “삭제한 코드는 주석 처리하지 않고 제거한다”는 기준을 세우면 이미 좋은 작은 커밋의 가치를 크게 높일 수 있다. 마지막으로 상태 필드의 불변식(예: `0 <= tmpHp <= fullHp`)을 setter 또는 전용 `takeDamage`/`heal` 메서드에 모으면, 이번에 경험한 체력·회피·max 처리 오류를 다음 기능에서도 예방할 수 있다. 즉 강점인 빠른 피드백 루프를, 재현 가능한 검증과 명확한 경계 위에서 더 단단하게 만드는 것이 다음 단계다.

## 우선순위 제안

1. **P0 — 규칙 테스트 5개부터 추가:** 회피, 피해 하한, HP/MP 상한, 이벤트 방문 제외, 이벤트 소진 종료.
2. **P1 — 폐기 주석과 명명 정리:** 주석 처리된 구 구현을 삭제하고 `isVisited`, `Draw...`, `Enhancement...`처럼 현재 역할을 바로 읽을 수 있게 통일.
3. **P1 — GameManager 분할:** 콘솔 출력/입력과 `runGame`의 이벤트 상태 전이를 별도 클래스로 분리.
4. **P2 — 데이터 계약 강화:** ID 검증, DB 초기화·새 게임 리셋 정책, 확률 규칙 문서화 및 난수 주입.

이 순서라면 이미 구현된 기능을 크게 흔들지 않으면서도, 7월 31일에 집중됐던 규칙·순서 오류를 재발 방지하는 기반을 먼저 확보할 수 있다.
